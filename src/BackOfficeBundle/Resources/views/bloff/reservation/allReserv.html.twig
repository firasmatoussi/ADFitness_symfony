{% extends 'base.html.twig' %}

{% block content %}
<div class="breadcrumbs">
    <div class="col-sm-4">
        <div class="page-header float-left">
            <div class="page-title">
                <h1>Gestion de reservation</h1>
            </div>
        </div>
    </div>
    <div class="col-sm-8">
        <div class="page-header float-right">
            <div class="page-title">
            </div>
        </div>
    </div>
</div>


<div class="content mt-3">
    <div class="animated fadeIn">
        <div class="row">

            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        <strong class="card-title">Table Reservation</strong>
                    </div>
                    <div class="card-body">

                        <table id="bootstrap-data-table-export" class="table table-striped table-bordered">
        <thead>
        <tr>
            <th>Libelle</th>
            <th>Username</th>
            <th>Email</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for reservation in reservations %}
            <tr>
                <td>{{ reservation.lib }}</td>
                <td>{{ reservation.username }}</td>
                <td>{{ reservation.email }}</td>
                <td>
                            <a class="btn btn-primary" href="{{ path('reservation_show_admin', { 'id': reservation.id }) }}">Afficher</a>
                            <a class="btn btn-warning" href="{{ path('reservation_edit_admin', { 'id': reservation.id }) }}">Modifier</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


    <a class="btn btn-primary" href="{{ path('reserv_index_admin') }}">Retourner à liste des cours</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
