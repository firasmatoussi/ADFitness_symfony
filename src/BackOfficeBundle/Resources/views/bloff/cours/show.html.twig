{% extends 'base.html.twig' %}

{% block content %}


    <div class="breadcrumbs">
        <div class="col-sm-4">
            <div class="page-header float-left">
                <div class="page-title">
                    <h1>Gestion de cours</h1>
                </div>
            </div>
        </div>
        <div class="col-sm-8">
            <div class="page-header float-right">
                <div class="page-title">
                    <ol class="breadcrumb text-right">

                    </ol>
                </div>
            </div>
        </div>
    </div>
<div class="content mt-3">
    <div class="animated fadeIn">
        <div class="row">

            <div class="col-md-12">
                <div class="card">

                    <div class="card-body">
    <table id="bootstrap-data-table-export" class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Libelle</th>
                <td>{{ cour.lib }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ cour.type }}</td>
            </tr>
            <tr>
                <th>Salle</th>
                <td>{{ cour.salle }}</td>
            </tr>
            <tr>
                <th>Nom du coach</th>
                <td>{{ cour.coachName }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{% if cour.date %}{{ cour.date|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Nombre de place</th>
                <td>{{ cour.nbPlace }}</td>
            </tr>
        </tbody>
    </table>

<table>
    <tr><td>
            <a class="btn btn-primary" role="button" href="{{ path('cours_index_admin') }}">Retourner à la liste des cours</a>
        </td>
    </tr>
    <tr>
        <td>
            <a class="btn btn-warning" role="button" href="{{ path('cours_edit_admin', { 'id': cour.id }) }}">Modifier</a>
        </td>
    </tr>
    <tr>
        <td>
            {{ form_start(delete_form) }}
            <input  type="submit" value="Delete" class="btn btn-danger" onclick="confirm('Voullez-vous supprimer ce cours?') || event.preventDefault()">
            {{ form_end(delete_form) }}
        </td>
    </tr>
</table>
        </div>
    </div>
</div>
        </div>
    </div>
</div>
{% endblock %}
