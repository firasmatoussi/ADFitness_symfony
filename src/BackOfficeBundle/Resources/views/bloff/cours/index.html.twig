{% extends 'base.html.twig' %}

{% block content %}

    <div class="breadcrumbs">
        <div class="col-sm-4">
            <div class="page-header float-left">
                <div class="page-title">
                    <h1>Gestion de cours</h1>
                </div>
            </div>
        </div>
        <div class="col-sm-8">
            <div class="page-header float-right">
                <div class="page-title">
                    <ol class="breadcrumb text-right">
                    </ol>
                </div>
            </div>
        </div>
    </div>


<div class="content mt-3">
    <div class="animated fadeIn">
        <div class="row">

            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        <strong class="card-title">Table Cours</strong>
                    </div>
                    <div class="card-body">

                        <form method="post" action="{{ path('coach_filtre') }}">
                            <label for="filtrer2">Chercher un entraîneur</label>
                            <select name="firas">
                                <option>Takoua Riahi</option>
                                <option>Houssem Ben Achour</option>
                                <option>Sabri Ben Marzouk</option>
                                <option>Amine Khemiri</option>
                                <option>Firas Matoussi </option>
                                <option>Walid Nsiri</option>


                            </select>
                            <input class="btn btn-danger" type="submit" value="Cherher">
                        </form>

                        <form method="post" action="{{ path('cours_filtre') }}">

                            <label for="filtrer">Chercher un cours</label>
                            <select name="choice">
                                <option name="opt">Musculation</option>
                                <option name="opt">Fitness</option>
                                <option name="opt"> Natation</option>
                                <option name="opt">Aerobic Sportive</option>
                                <option name="opt">Lutte Libre</option>
                                <option name="opt"> Boxe</option>
                            </select>
                            <input type="submit" value="Chercher" class="btn btn-success" name="filtre_btn">
                        </form>


                        <br>
                        <a class="btn btn-primary" role="button" href="{{ path('cours_new_admin') }}">Ajouter un nouveau Cours</a>

                       {# <form method="post" action="{{ path('cours_filtre') }}">
                            <label for="filtrer">Filtrer</label>
                            <input type="date" name="datecours">
                            <input type="submit" class="btn btn-warning" name="filtre_btn">
                        </form>#}


                        <table id="bootstrap-data-table-export" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Libelle</th>
                <th>Type</th>
                <th>Salle</th>
                <th>Nom du Coach</th>
                <th>Date</th>
                <th>Nombre de place</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cour in cours %}
            <tr>
                <td>{{ cour.lib }}</td>
                <td>{{ cour.type }}</td>
                <td>{{ cour.salle }}</td>
                <td>{{ cour.coachName }}</td>
                <td>{% if cour.date %}{{ cour.date|date('Y-m-d') }}{% endif %}</td>
                <td>{{ cour.nbPlace }}</td>
                <td>
                            <a class="btn btn-primary" role="button" href="{{ path('cours_show_admin', { 'id': cour.id }) }}">Afficher</a>

                            <a class="btn btn-warning" role="button" href="{{ path('cours_edit_admin', { 'id': cour.id }) }}">Modifier</a>

                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>





                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


{% endblock %}
